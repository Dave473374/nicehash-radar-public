{"name":"Test NiceHash Private API","on":{"workflow_dispatch":{}},"jobs":{"test":{"runs-on":"ubuntu-latest","steps":[{"uses":"actions/checkout@v4"},{"uses":"actions/setup-python@v5","with":{"python-version":"3.x"}},{"run":"pip install requests"},{"name":"Fetch private EasyMining history","run":"python3 scripts/collect_completed_orders.py","env":{"NICEHASH_API_KEY":"${{ secrets.NICEHASH_API_KEY }}","NICEHASH_API_SECRET":"${{ secrets.NICEHASH_API_SECRET }}","NICEHASH_ORG_ID":"${{ secrets.NICEHASH_ORG_ID }}"}},{"name":"Show available field names","run":"python3 -c \"import json; d=json.load(open('/tmp/nicehash-completed-orders.json')); rows=d.get('list',[]); print('Rows:',len(rows)); print('Top-level fields:',sorted(rows[0].keys()) if rows else [])\""}]}}}
